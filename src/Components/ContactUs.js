import { Outlet } from "react-router-dom";

const ContactUs = () => {
    return (
        <div>
            <h4>Reach out to us on</h4>
            <Outlet/>
        </div>
    )
}

export default ContactUs;